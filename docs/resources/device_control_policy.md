---
page_title: "crowdstrike_device_control_policy Resource - crowdstrike"
subcategory: "Host Setup and Management"
description: |-
  Manages CrowdStrike Device Control policies that control USB device access on endpoints.
  API Scopes
  The following API scopes are required:
  Device Control Policies | Read & Write
---

# crowdstrike_device_control_policy (Resource)

Manages CrowdStrike Device Control policies that control USB device access on endpoints.

## API Scopes

The following API scopes are required:

- Device Control Policies | Read & Write


## Example Usage

```terraform
resource "crowdstrike_device_control_policy" "example" {
  name          = "example-device-control-policy"
  description   = "Manage USB mass storage access"
  platform_name = "Windows"
  enabled       = true

  settings = {
    enforcement_mode      = "MONITOR_ENFORCE"
    end_user_notification = "NOTIFY_USER"

    classes = [
      {
        id     = "MASS_STORAGE"
        action = "BLOCK_ALL"

        exceptions = [
          {
            combined_id = "1234_5678_SERIAL123"
            action      = "FULL_ACCESS"
            description = "Approved USB device"
          }
        ]
      },
      {
        id     = "WIRELESS"
        action = "BLOCK_ALL"
      }
    ]

    custom_notifications = {
      blocked_notification = {
        use_custom     = true
        custom_message = "USB access is blocked by policy"
      }
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the device control policy.
- `platform_name` (String) Platform for the device control policy. One of: `Windows`, `Mac`, `Linux`. Changing this value requires replacing the resource.
- `settings` (Attributes) Device control policy settings. (see [below for nested schema](#nestedatt--settings))

### Optional

- `clone_id` (String) Policy ID to clone settings from at creation time. Changing this value requires replacing the resource.
- `description` (String) Description of the device control policy.
- `enabled` (Boolean) Whether the policy is enabled.
- `host_groups` (Set of String) Host group IDs to attach to the policy.

### Read-Only

- `id` (String) Identifier for the device control policy.
- `last_updated` (String) Timestamp of the last Terraform update of the resource.

<a id="nestedatt--settings"></a>
### Nested Schema for `settings`

Required:

- `classes` (Attributes List) USB class policy configurations. (see [below for nested schema](#nestedatt--settings--classes))
- `end_user_notification` (String) Whether end users receive notifications. One of: `SILENT`, `NOTIFY_USER`.
- `enforcement_mode` (String) How the policy is enforced. One of: `MONITOR_ONLY`, `MONITOR_ENFORCE`.

Optional:

- `custom_notifications` (Attributes) Custom end-user notifications. (see [below for nested schema](#nestedatt--settings--custom_notifications))
- `enhanced_file_metadata` (Boolean) Enable enhanced file metadata on the sensor.

<a id="nestedatt--settings--classes"></a>
### Nested Schema for `settings.classes`

Required:

- `action` (String) Action for this USB class. One of: `FULL_ACCESS`, `BLOCK_ALL`, `BLOCK_EXECUTE`, `BLOCK_WRITE_EXECUTE`.
- `id` (String) USB class ID. One of: `ANY`, `AUDIO_VIDEO`, `IMAGING`, `MASS_STORAGE`, `MOBILE`, `PRINTER`, `WIRELESS`.

Optional:

- `exceptions` (Attributes List) Exceptions to class actions. (see [below for nested schema](#nestedatt--settings--classes--exceptions))

<a id="nestedatt--settings--classes--exceptions"></a>
### Nested Schema for `settings.classes.exceptions`

Optional:

- `action` (String) Exception-specific action override.
- `combined_id` (String) Combined identifier in the format `vendorID_productID_serialNumber`.
- `description` (String) Exception description. Maximum length: 512.
- `expiration_time` (String) Expiration timestamp in RFC3339 format.
- `product_id` (String) Hexadecimal product ID.
- `product_id_decimal` (String) Decimal product ID.
- `product_name` (String) Product name.
- `serial_number` (String) USB serial number. Maximum length: 126.
- `use_wildcard` (Boolean) Enable wildcard matching for serial numbers.
- `vendor_id` (String) Hexadecimal vendor ID.
- `vendor_id_decimal` (String) Decimal vendor ID.
- `vendor_name` (String) Vendor name.

Read-Only:

- `id` (String) API-generated identifier for the exception.



<a id="nestedatt--settings--custom_notifications"></a>
### Nested Schema for `settings.custom_notifications`

Optional:

- `blocked_notification` (Attributes) Custom notification displayed when a USB device is blocked. (see [below for nested schema](#nestedatt--settings--custom_notifications--blocked_notification))
- `restricted_notification` (Attributes) Custom notification displayed when a USB device is restricted. (see [below for nested schema](#nestedatt--settings--custom_notifications--restricted_notification))

<a id="nestedatt--settings--custom_notifications--blocked_notification"></a>
### Nested Schema for `settings.custom_notifications.blocked_notification`

Required:

- `use_custom` (Boolean) Whether to use a custom message.

Optional:

- `custom_message` (String) Custom message text. Maximum length: 256.


<a id="nestedatt--settings--custom_notifications--restricted_notification"></a>
### Nested Schema for `settings.custom_notifications.restricted_notification`

Required:

- `use_custom` (Boolean) Whether to use a custom message.

Optional:

- `custom_message` (String) Custom message text. Maximum length: 256.

## Import

Import is supported using the following syntax:

```shell
terraform import crowdstrike_device_control_policy.example <device_control_policy_id>
```
